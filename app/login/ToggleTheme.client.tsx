"use client";

import Button from '@components/client/Button'

const ToggleTheme = () => {
  const handlePress = () => {

  };

  return <Button onPress={handlePress}>Toggle Theme</Button>
};
export default ToggleTheme;
